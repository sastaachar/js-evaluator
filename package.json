{
  "name": "js-evaluator-repo",
  "private": true,
  "type": "module",
  "description": "Workspace root. The publishable package lives in packages/js-evaluator; docs/ is the GitHub Pages site and is never published.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync": "node scripts/sync-site.mjs",
    "check:sync": "node scripts/sync-site.mjs --check",
    "site": "npm run sync && npx --yes serve docs",
    "release": "npm run sync && npm publish -w js-evaluator"
  },
  "engines": {
    "node": ">=18"
  }
}
